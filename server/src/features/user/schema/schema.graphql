type User {
    id: ID!
    name: String!
}

type UserWithToken {
    id: ID!
    name: String!
    token: String!
}

input CreateUserInput {
    name: String!
    password: String!
}

type Query {
    users: [User]!
}

type Mutation {
    signup(input: CreateUserInput): UserWithToken!
}
