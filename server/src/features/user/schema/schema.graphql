type User {
    id: ID!
    name: String!
    password: String!
}

input CreateUserInput {
    name: String!
    password: String!
}

type Query {
    users: [User]!
}

type Mutation {
    signup(input: CreateUserInput): User!
}
